% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fun_interpolate_track.R
\name{atl_interpolate_track}
\alias{atl_interpolate_track}
\title{Interpolate a tracking data}
\usage{
atl_interpolate_track(
  data,
  tag = "tag",
  x = "x",
  y = "y",
  time = "time",
  patch = "patch",
  interp_interval = 60,
  max_gap = NULL,
  max_dist = NULL,
  patches_only = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing tracking data.}

\item{tag}{Character. Name of the column containing tag or individual IDs.
Defaults to \code{"tag"}.}

\item{x}{Character. Name of the column containing x coordinates.
Defaults to \code{"x"}.}

\item{y}{Character. Name of the column containing y coordinates.
Defaults to \code{"y"}.}

\item{time}{Character. Name of the column containing UNIX timestamps
(numeric, in seconds). Defaults to \code{"time"}.}

\item{patch}{Character. Name of the column containing residence patch IDs.
Defaults to \code{"patch"}. Only used when \code{patches_only = TRUE}.}

\item{interp_interval}{Numeric. The time interval in seconds to interpolate
to. Defaults to \code{60}.}

\item{max_gap}{Numeric. Maximum gap in seconds between two observed positions
for which interpolation is performed. Gaps larger than this value will not
be interpolated. Defaults to \code{NULL}.}

\item{max_dist}{Numeric or \code{NULL}. Maximum distance in coordinate units
between two observed positions for which interpolation is performed. If
\code{NULL} (default), no distance filter is applied.}

\item{patches_only}{Logical. If \code{TRUE} (default), interpolation is
restricted to gaps within the same patch ID. Requires the column specified
in \code{patch} to be present in \code{data}.}
}
\value{
A \code{data.table} with the same columns as the input, plus \code{datetime}
(POSIXct timestamp in UTC), \code{gap_next} (time in seconds to the next
observed position), \code{interpolated} (logical, \code{TRUE} for interpolated
rows), and optionally \code{dist_next} (distance to the next observed position,
if \code{max_dist} is set). Rows are ordered by tag and time.
}
\description{
This function interpolates gaps in the tracking data within a set time
interval (\code{interp_interval}) and within defined temporal and spatial
restrictions. Cooridnates in gaps a filled with a simple linear interpolation
using \code{zoo::na.approx()}. One is required to specify the maximal time gap
(\code{max_gap}) between positions that will be interpolated and can additionally
specify a maximal distance between positions (\code{max_dist}) to restrict
interpolation to more local movements. If \code{patches_only = TRUE},
interpolation is further restricted to only gaps within residence patches.
}
\details{
Best to use with already thinned data (e.g. with \code{atl_thin_data()}) to avoid
unnecessary interpolation of very fine-scale data and to speed up processing.
}
\author{
Johannes Krietsch
}
